import React from 'react';

function MainPage() {
    return (
        <main className="app-container page-content">
            <h1 className="page-title">Main page</h1>
            <p className="contact-meta">Welcome to the contacts demo app — create and manage simple contacts stored in localStorage.</p>
        </main>
    );
}

export default MainPage;